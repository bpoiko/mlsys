#include <cuda_runtime.h>

int main() {
    // TODO: implement only after writing DESIGN.md and freezing correctness tests.
    // Keep a measured baseline; optimize exactly one profiled bottleneck at a time.
}
