# Questions — Language Foundations

1. How does a reference differ from an owning pointer?
2. What does a trailing `const` on a member function promise?
3. Can a const reference extend every temporary's lifetime safely?
4. Which order destroys local objects and members?
5. What invariant should hold after every public member call?
6. When is pass-by-value preferable to pass-by-reference?
