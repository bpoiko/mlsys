#include <atomic>
#include <cstddef>
#include <mutex>
#include <thread>
#include <vector>

struct Counters {
    // TODO: choose fields/layout for baseline and false-sharing experiments.
};

int main() {
    // TODO: implement correctness-first mutex/atomic/partitioned experiments.
}
