# Questions — Concurrency and Cache

1. What makes two memory accesses a data race?
2. What ordering does a mutex establish?
3. When is an atomic counter insufficient for a larger invariant?
4. What is false sharing and why can padding help?
5. Why can more threads reduce throughput?
6. How do AoS and SoA differ for cache and SIMD access?
