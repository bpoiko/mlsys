#include <algorithm>
#include <cstddef>
#include <vector>

template <class InputIt, class OutputIt, class UnaryOp>
void transform_range(InputIt first, InputIt last, OutputIt output, UnaryOp op) {
    // TODO: implement using iterator operations promised by the interface.
}

int main() {
    // TODO: exercise a vector, const iteration, a lambda, and edge cases.
}
