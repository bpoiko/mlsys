# Questions — Containers and Generic Code

1. When does `vector` reallocation invalidate pointers and iterators?
2. How do `size` and `capacity` differ?
3. What does a template require from an iterator even without a named concept?
4. How do value and reference lambda captures affect lifetime?
5. Why is contiguous storage valuable for SIMD and device transfer?
6. When can a standard algorithm express intent better than a raw loop?
