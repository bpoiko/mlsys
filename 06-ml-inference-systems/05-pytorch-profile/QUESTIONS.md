# Questions — PyTorch Profiling

1. Why can ordinary Python timing underreport CUDA execution time?
2. What costs belong to first-run versus steady-state behavior?
3. How can a tensor layout trigger hidden copies?
4. What does a CPU/GPU trace reveal that a kernel table does not?
5. Which tradeoffs accompany compile/fusion and lower precision?
6. How do you know the optimized path still computes the intended model?
