# 06 — ML Inference Systems

This stage treats a model as shapes, operations, bytes, dependencies, queues, and
latency/throughput tradeoffs. Order: transformer execution → attention/KV cache →
prefill/decode/batching → precision/quantization → framework profiling. Derive a
prediction before collecting a trace.
