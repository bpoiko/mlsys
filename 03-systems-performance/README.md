# 03 — Systems and Performance

This stage turns performance work into experimental practice. Order: memory
hierarchy → benchmark validity → Linux `perf` → SIMD/branches → quantitative
models. Each result needs correctness, raw samples, environment, hypothesis, and
a causal explanation.
