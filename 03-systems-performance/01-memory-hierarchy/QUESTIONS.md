# Questions — Memory Hierarchy

1. Distinguish cache line, cache set, and cache capacity.
2. Why can sequential access approach bandwidth while pointer chasing exposes latency?
3. What does virtual-to-physical translation cache in a TLB?
4. Why can large pages change results?
5. Which prefetcher effects can confound a stride benchmark?
6. Why is working-set size part of the benchmark specification?
