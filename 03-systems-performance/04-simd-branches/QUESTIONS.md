# Questions — SIMD and Branches

1. What dependencies can prevent loop vectorization?
2. Why do aliasing assumptions matter to the compiler?
3. How is a vector tail handled?
4. Why does input distribution affect branch results?
5. What is the difference between SIMD lanes and GPU SIMT threads?
6. When can branchless code be slower?
