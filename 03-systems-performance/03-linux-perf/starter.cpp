#include <cstddef>
#include <cstdint>
#include <span>

std::uint64_t workload(std::span<const std::uint32_t> input, int mode) {
    // TODO: implement correctness-equivalent modes with different access/control flow.
}

int main(int argc, char **argv) {
    // TODO: select mode/size, run enough work, and print a checksum.
}
