# Questions — Performance Models

1. What exactly belongs in arithmetic-intensity byte traffic?
2. Where is the Roofline ridge point?
3. How does tiling change arithmetic intensity?
4. Why may achieved bandwidth be below the bandwidth ceiling?
5. What fraction in Amdahl's Law must be measured end-to-end?
6. How can optimizing a kernel produce little application speedup?
