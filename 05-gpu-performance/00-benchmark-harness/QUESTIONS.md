# Questions — GPU Benchmark Harness

1. On which stream must events be recorded to time intended work?
2. Why is kernel-launch time not kernel-execution time?
3. When is minimum useful, and why is it not enough?
4. How should clock warmup and thermal drift be reported?
5. What bytes count toward achieved bandwidth for vector add?
6. Which overhead belongs in kernel latency versus end-to-end latency?
