# Questions — Structs

1. Why may `sizeof(struct)` exceed the sum of member sizes?
2. Why can trailing padding exist?
3. When can member reordering reduce size?
4. What does `->` mean in terms of dereference and member access?
5. What ABI or compatibility risks come with packing a struct?
6. When is pass-by-value desirable despite copying?
