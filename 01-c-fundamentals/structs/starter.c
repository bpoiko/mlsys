#include <stddef.h>
#include <stdio.h>

/* TODO: define Point and two differently ordered layout-experiment structs. */

int main(void) {
    /* TODO: print sizeof and offsetof results, then compare with predictions. */
}
