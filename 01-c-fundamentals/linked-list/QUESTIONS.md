# Questions — Linked List

1. Why do operations that may replace the head need `Node **` or a returned head?
2. How do you avoid losing the remainder of a list while freeing a node?
3. Which invariants cover empty and singleton lists?
4. What is the ownership status of a traversal pointer?
5. Compare contiguous arrays and linked lists for cache behavior.
6. Why might a linked list be a poor GPU data structure?
