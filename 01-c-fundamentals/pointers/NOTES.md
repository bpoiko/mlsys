# Notes — Pointers

- Explain `p`, `*p`, and `&p` using types and values:
- Predict each address/value before running:
- Explain pointer arithmetic without assuming byte sizes:
- What evidence shows that `swap` modifies its caller?
- Remaining warnings and what each one teaches:
