# Questions — Pointers

1. Why does C pass the pointer value by value yet allow mutation of the pointee?
2. When is subtracting two pointers defined?
3. What is the type and meaning of `&p` when `p` is `int *`?
4. Why is one-past-the-end formable but not dereferenceable?
5. What does the pointed-to type change about `p + 1`?
6. When is a double pointer necessary?
