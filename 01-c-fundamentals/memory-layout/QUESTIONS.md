# Questions — Memory Layout

1. What constraints determine a member's offset?
2. Why does an array require each element to have the same stride?
3. What is the difference between size and alignment?
4. How does row-major order affect adjacent addresses?
5. What makes an aligned vector load possible or unsafe?
6. Which layout facts are language guarantees and which are ABI choices?
