#include <stddef.h>
#include <stdalign.h>
#include <stdio.h>
#include <stdlib.h>

/* TODO: define two equivalent structs with different field order. */

size_t flat_index(size_t row, size_t column, size_t columns) {
    /* TODO: calculate the row-major index. */
}

int main(void) {
    /* TODO: predict/print layout and compare two matrix traversal orders. */
}
