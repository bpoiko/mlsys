#include <stddef.h>

size_t my_strlen(const char *text) {
    /* TODO: return the number of characters before the terminator. */
}

int my_strcmp(const char *lhs, const char *rhs) {
    /* TODO: implement the same sign contract as strcmp. */
}

char *my_strcpy(char *destination, const char *source) {
    /* TODO: copy through and including the terminator. */
}

void reverse_in_place(char *text) {
    /* TODO: reverse a valid mutable C string without allocating. */
}

int main(void) {
    /* TODO: add edge cases and compare with the standard library. */
}
