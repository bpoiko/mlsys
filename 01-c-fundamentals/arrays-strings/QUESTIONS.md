# Questions — Arrays and Strings

1. Why can `sizeof array` differ from `sizeof parameter` inside a function?
2. What storage is required for the string `"GPU"`?
3. Why is modifying a string literal undefined?
4. How do overlap requirements differ between `memcpy` and `memmove`?
5. Which edge cases distinguish a correct `strcmp` from equality-only testing?
6. Derive the address of element `(row, col)` in a flat row-major matrix.
