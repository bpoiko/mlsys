# Questions — Streams and Events

1. What ordering is guaranteed within one stream?
2. What permits operations in different streams to overlap?
3. Why may pageable host memory prevent expected async behavior?
4. Why are host clocks easy to misuse for kernel timing?
5. What does an event synchronize relative to?
6. Distinguish pipeline latency from steady-state throughput.
