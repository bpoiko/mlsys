#include <cuda_runtime.h>
#include <cstddef>

int main() {
    // TODO: create serialized baseline, then chunked async copies/kernel/copies.
    // TODO: use explicit streams/events and preserve all resource lifetimes.
}
