# Questions — Reduction

1. Why is floating-point addition not associative?
2. What deadlock risk appears when only some threads reach `__syncthreads()`?
3. Why do non-power-of-two sizes expose bugs?
4. What work and traffic should one block perform?
5. When are warp-level primitives appropriate?
6. Why might fewer barriers not improve end-to-end time?
