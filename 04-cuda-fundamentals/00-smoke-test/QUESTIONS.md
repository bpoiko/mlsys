# Questions — CUDA Smoke Test

1. Which code runs on the host and which on the device?
2. Why can a kernel launch return before its work finishes?
3. What are `threadIdx`, `blockIdx`, `blockDim`, and `gridDim`?
4. Why check both launch and execution errors?
5. What is a warp, and how does it relate to a block?
6. Which device facts constrain a valid launch?
