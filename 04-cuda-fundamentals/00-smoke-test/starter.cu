#include <cuda_runtime.h>
#include <cstdio>

__global__ void identify_threads() {
    // TODO: print block/thread coordinates and a global index.
}

int main() {
    // TODO: launch, check launch status, synchronize, check execution, and exit.
}
