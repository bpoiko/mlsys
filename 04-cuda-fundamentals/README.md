# 04 — CUDA Fundamentals

Follow numeric order. Correctness against a CPU reference comes before timing;
Compute Sanitizer and CUDA error checks are part of correctness. Record device,
driver, toolkit, compile flags, dimensions, and raw timings. Existing READMEs were
moved intact from the earlier `02-cuda-fundamentals` path.
