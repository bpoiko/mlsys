# Questions — Naive Matmul

1. Derive the three row-major index expressions for MxK times KxN.
2. Why is exact float equality often inappropriate?
3. Which matrix access is naturally coalesced in a simple mapping?
4. How many floating operations compute one output element?
5. Why is calling every naive matmul “memory-bound” too broad?
6. What changes for rectangular and non-divisible dimensions?
