# Questions — Vector Add

1. Why round the block count up and still use a bounds check?
2. What is invalid about dereferencing a device pointer in ordinary host code?
3. Which direction does each `cudaMemcpyKind` describe?
4. How many bytes does vector add minimally transfer per element?
5. Why might 128 and 256 threads per block perform similarly?
6. Which operations synchronize the host implicitly?
